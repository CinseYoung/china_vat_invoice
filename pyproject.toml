[project]
name = "china_vat_invoice"
authors = [{ name = "CinseYoung", email = "dev@example.com" }]
description = "ERPNext 中国本地化: 增值税发票出票 (241×140mm)"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<17.0.0"
erpnext = ">=15.0.0,<17.0.0"
